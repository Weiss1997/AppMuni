@model IEnumerable<AppPlanillasAlumnos.Models.DatosAlumnos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatosAlumnosNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatosAlumnosApellido)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DatosAlumnosNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatosAlumnosApellido)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DatosAlumnosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DatosAlumnosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DatosAlumnosID })
        </td>
    </tr>
}

</table>
